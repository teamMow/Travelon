
<div class="field">
	<%= f.label :image %>
	<%= f.file_field :image %>
</div>

<div class="field">
	<%= f.label :user_id %><br>
	ユーザーID
	<%= f.collection_select :user_id, User.all, :id, :id %>
</div>