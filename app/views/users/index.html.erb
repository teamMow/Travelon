<h1>ユーザー一覧</h1>
<% @users.each do |user| %>
	<p>名前</p>
	<span><%= link_to user.name, user_path(user.id) %></span>

<% end %>

<%= paginate @users %>
<p><%= link_to "記事一覧", articles_path %></p>


<h2><%= link_to "投稿一覧", root_path %></h2>
